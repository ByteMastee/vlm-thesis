<?xml version="1.0"?>

<robot xmlns:xacro="http://www.ros.org/wiki/xacro">

  <!-- ================= ARGUMENT ================= -->

  <xacro:arg name="robot_name" default="robot1"/>

  <!-- ================================================= -->
  <!-- ROS2 CONTROL -->
  <!-- ================================================= -->

  <gazebo>
    <plugin filename="libgazebo_ros2_control.so"
            name="gazebo_ros2_control"/>
  </gazebo>

  <ros2_control name="GazeboSystem" type="system">
    <hardware>
      <plugin>gazebo_ros2_control/GazeboSystem</plugin>
    </hardware>

    <joint name="joint_front_left_wheel">
      <command_interface name="velocity"/>
      <state_interface name="velocity"/>
    </joint>

    <joint name="joint_front_right_wheel">
      <command_interface name="velocity"/>
      <state_interface name="velocity"/>
    </joint>
  </ros2_control>

  <!-- ================================================= -->
  <!-- D435 DEPTH CAMERA -->
  <!-- ================================================= -->

  <gazebo reference="link_d435_camera">

    <sensor name="d435" type="depth">
      <update_rate>10</update_rate>

      <camera>
        <horizontal_fov>1.0472</horizontal_fov>
        <image>
          <width>640</width>
          <height>480</height>
          <format>R8G8B8</format>
        </image>
        <clip>
          <near>0.05</near>
          <far>3.0</far>
        </clip>
      </camera>

      <plugin filename="libgazebo_ros_depth_camera.so" name="d435_plugin">

        <frame_name>d435_link</frame_name>

        <image_topic_name>color/image_raw</image_topic_name>
        <camera_info_topic_name>color/camera_info</camera_info_topic_name>

        <depth_image_topic_name>depth/image_raw</depth_image_topic_name>
        <depth_camera_info_topic_name>depth/camera_info</depth_camera_info_topic_name>

        <point_cloud>true</point_cloud>

        <ros>
          <namespace>$(arg robot_name)/d435</namespace>
        </ros>

      </plugin>
    </sensor>
  </gazebo>

  <!-- ================================================= -->
  <!-- T265 FISHEYE -->
  <!-- ================================================= -->

  <gazebo reference="link_t265_camera_fisheye">

    <sensor name="t265_fisheye" type="wideanglecamera">

      <camera>

        <horizontal_fov>6.283</horizontal_fov>

        <image>
          <width>800</width>
          <height>848</height>
        </image>

        <clip>
          <near>0.1</near>
          <far>100</far>
        </clip>

        <lens>
          <type>custom</type>

          <custom_function>
            <c1>1.05</c1>
            <c2>4</c2>
            <f>1.0</f>
            <fun>tan</fun>
          </custom_function>

          <scale_to_hfov>true</scale_to_hfov>
          <cutoff_angle>3.1415</cutoff_angle>
        </lens>

        <always_on>true</always_on>
        <update_rate>30</update_rate>

      </camera>

      <plugin filename="libgazebo_ros_camera.so" name="t265_camera">

        <frame_name>t265_link</frame_name>

        <image_topic_name>fisheye/image_raw</image_topic_name>
        <camera_info_topic_name>fisheye/camera_info</camera_info_topic_name>

        <ros>
          <namespace>$(arg robot_name)/t265</namespace>
        </ros>

      </plugin>

    </sensor>
  </gazebo>

  <!-- ================================================= -->
  <!-- IMU -->
  <!-- ================================================= -->

  <gazebo reference="link_t265_camera_gyro">

    <sensor name="imu" type="imu">

      <always_on>true</always_on>
      <update_rate>30</update_rate>

      <plugin filename="libgazebo_ros_imu_sensor.so" name="imu_plugin">

        <frame_name>t265_link</frame_name>

        <ros>
          <namespace>$(arg robot_name)/t265</namespace>
        </ros>

      </plugin>

    </sensor>
  </gazebo>

  <!-- ================================================= -->
  <!-- SONAR MACRO -->
  <!-- ================================================= -->

  <xacro:macro name="sonar" params="link frame topic">

    <gazebo reference="${link}">

      <sensor type="ray" name="${frame}_sonar">

        <update_rate>5</update_rate>

        <ray>
          <range>
            <min>0.01</min>
            <max>0.75</max>
          </range>
        </ray>

        <plugin filename="libgazebo_ros_range.so" name="${frame}_range">

          <frame_name>${frame}</frame_name>

          <ros>
            <namespace>$(arg robot_name)/ultrasound</namespace>
          </ros>

          <topic_name>${topic}</topic_name>

        </plugin>

      </sensor>
    </gazebo>

  </xacro:macro>

  <!-- SONARS -->

  <xacro:sonar link="link_rear_sonar" frame="rear" topic="rear"/>
  <xacro:sonar link="link_side_left_sonar" frame="left" topic="left"/>
  <xacro:sonar link="link_side_right_sonar" frame="right" topic="right"/>
  <xacro:sonar link="link_front_left_sonar" frame="front_left" topic="front_left"/>
  <xacro:sonar link="link_front_right_sonar" frame="front_right" topic="front_right"/>

</robot>
